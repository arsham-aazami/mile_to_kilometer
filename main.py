from tkinter import *
 
window = Tk()
window.title("Mile to Km convertor")
window.minsize(450, 450)
window.config(padx=30, pady=30)

entrty = Entry(width=20)
entrty.grid(column=0, row=0)

# lable = Label(text="Miles is equal to")
# lable.grid(column=1, row=0)




window.mainloop()